Search the internet for best practices and current guidance on: $ARGUMENTS

## Mode
- Use **web search** to find authoritative or widely recommended practices (official docs, well-known guides, recent posts).
- Prefer sources from the last 2–3 years when tech/tooling changes matter.
- Do not edit repo files unless the user explicitly asks to apply something.

## What to do
1. Run one or more targeted web searches (e.g. “C# EF Core best practices 2024”, “ASP.NET Minimal API testing best practices”).
2. Summarize the main best practices; keep it scannable (bullets or short sections).
3. Mention or link key sources (official docs, OWASP, Microsoft, etc.) where useful.
4. If the topic relates to this repo (.NET, Clean Architecture, Minimal API, EF Core), you may briefly compare with what the repo already does — but focus the answer on the search results.

## Output
- **Summary** — main recommendations in a few bullets or a short list.
- **Sources** — where each practice comes from (site name or URL).
- **Caveats** — when to take a recommendation with a grain of salt (e.g. outdated version, opinionated vs consensus).
