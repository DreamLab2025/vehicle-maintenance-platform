Nhiệm vụ: Phân tích thói quen sử dụng xe và trả về hệ số điều chỉnh (usageAdjustmentFactor) dựa trên thông tin Q&A của người dùng. Hệ thống đã tính toán baseline dự đoán — nhiệm vụ của bạn là điều chỉnh dựa trên thực tế sử dụng.

---
Today: [[TODAY]] | Xe: [[VEHICLE_NAME]] | Current_ODO (C): [[CURRENT_ODO]] km | Mua: [[PURCHASE_DATE]]

Baseline dự đoán từ hệ thống:
[[BASE_PREDICTION]]

Thói quen lái xe:
[[DRIVING_PATTERN]]

Q&A người dùng (ưu tiên cao nhất):
[[ANSWER_BLOCK]]

Lịch hãng (tham khảo): [[SCHEDULE_BLOCK]]

---
Hướng dẫn:

1) Phân tích Q&A để xác định mức độ sử dụng so với bình thường:
   - Địa hình khó (đèo, đường xấu, bụi nhiều) → tăng tần suất bảo dưỡng (factor < 1.0)
   - Tải nặng, chạy liên tục, thời tiết khắc nghiệt → factor < 1.0
   - Sử dụng nhẹ nhàng, đường tốt, thành phố → factor gần 1.0
   - Không có thông tin → usageAdjustmentFactor = 1.0 (dùng baseline)

2) usageAdjustmentFactor nằm trong khoảng 0.7–1.3:
   - 0.7–0.9: sử dụng nặng, cần bảo dưỡng sớm hơn
   - 0.9–1.1: sử dụng bình thường
   - 1.1–1.3: sử dụng nhẹ, có thể bảo dưỡng muộn hơn

3) lastServiceOdometer / lastServiceDate: Suy từ Q&A nếu có ("X km trước", "Z tháng trước", "tại Y km"...). Null nếu không suy được.

4) needsImmediateAttention: true khi xe đã vượt mốc bảo dưỡng dự kiến (current_odo >= baseline predicted odo).

5) reasoning: Giải thích ngắn bằng tiếng Việt về thói quen sử dụng, lý do chọn adjustment factor, và tình trạng hiện tại.

6) warnings: Cảnh báo nếu xe đã quá hạn bảo dưỡng hoặc có dấu hiệu bất thường. Mảng rỗng nếu không có gì.

Output (JSON only, no markdown):
{"recommendations":[{"partCategorySlug":"[[PART_CATEGORY_SLUG]]","lastServiceOdometer":number|null,"lastServiceDate":"yyyy-MM-dd"|null,"usageAdjustmentFactor":0.7-1.3,"reasoning":"string","needsImmediateAttention":bool,"warnings":["string"]}]}
